@model MVCWEB.Models.UserAuthentif



@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout_authentification.cshtml";
}
@section test2{

    @using (Html.BeginForm("Connect", "Authentification", FormMethod.Post))
    {
        @*@Html.AntiForgeryToken()*@

        <hr />
        <p>Veuillez saisir votre login et mot de passe de votre session Windows </p>
        <div class="form-horizontal well">

            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                @*<div class="col-xs-12">
                    @Html.Label("Votre Login Windows", htmlAttributes: new { @class = "control-label col-xs-12", style = "font-size:15px;font-family: Arial;font-weight:Bold" })
                </div>*@
                <div class="col-xs-12">
                    @Html.EditorFor(model => model.login, new { htmlAttributes = new { @class = "form-control", @placeholder = "Votre login Windows" } })
                    @Html.ValidationMessageFor(model => model.login, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @*<div class="col-xs-12">
                    @Html.Label("Mot de Passe", htmlAttributes: new { @class = "control-label col-xs-12", style = "font-size:15px;font-family:Arial;font-weight:Bold" })
                </div>*@
                <div class="col-xs-12">
                    @Html.EditorFor(model => model.password, new { htmlAttributes = new { @class = "form-control", @type = "password" , @placeholder = "Mot de passe" } })
                    @Html.ValidationMessageFor(model => model.password, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-lg-12">
                    <input type="submit" value="Login" class="btn btn-primary" style="width:230px;" />
                </div>
            </div>
            
            @{
                ViewBag.Title = "Test me";

            }
            <font color="@ViewBag.color">@ViewBag.message</font>
        </div>
        
                }

}
